# 出力先フォルダ

この `README.md` ファイルは、このフォルダに関しての説明を記述したファイルです。

## 概要

- このフォルダは、各ツールを用いて**生成した結果**を出力する為のフォルダです。
- [開発フォルダ](../README.md)の下位フォルダとする事を、想定しています。
  - フォルダ構成は「[フォルダ構成・方針](../Document/00_Materials/folder_policy.md)」で記しています。
- 出力結果をその場で検証できる様に、関連ファイルも保持します。(関連ファイルは、出力処理によって誤って削除, 更新されても問題ないように、控えを用意する。)
- ここで検証を行い、その後に配布, 送信用のフォルダにコピーする。
- 保持する各フォルダは、運用する環境と名称, 相対位置などを同じにします。
- 名称: **Dest**, 区分: **De** です。

## 使い方

- npmスクリプト(package.json)での記述の例

    ```json
    "scripts": {
      "pug": "pug ./Src/Pug/index.pug --out ./Dest/ --obj ./Src/Data/pug.json --extension htm --pretty",
    }
    ```

## 内部規約

1. 主に [コーディングルール](../Document/20_Design/codingrules.md) に準じます。
2. 運用先で使うために生成した成果物なので、その運用先の命名規則に準じます。
3. このフォルダのフォルダ区分は、'**De**'(Dest) です。
    1. 運用先の命名規則に準じるので、たぶん使わない。
    2. 一応、決め事として用意だけしています。
4. アップロード用フォルダへ、生成した結果の**直接の出力**を禁じます。
    1. 生成した結果の出力先は、ここです。
    2. ここで検証した後に、アップロード用フォルダへコピーします。
5. 原則として運用する環境と異なるフォルダ, ファイルを置くことは禁止する。
    1. この `README.md` ファイルなどの、説明用のファイルは配置してよい。
    2. 検証の為に一時的なセーブファイルを置くことも良いが、使用後は片づける事。
    3. 出力先フォルダなので、当然ですがこの環境で生成したものの出力先とすることは問題ないです。あくまで他所のファイルを置く事を制限しています。そのために検証に影響の可能性はあるが、そこまで気にするのなら別途に検証用フォルダを作成しましょう。(アップロード用フォルダでの検証で充分だと思います。)

## 作成者

Copyright(C) 2022-2025 KOUTAN
